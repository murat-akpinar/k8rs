## [unreleased]

### 🚀 Features

- *(rules)* See workloads whose pods were never created ([4ea285d](https://github.com/murat-akpinar/k8rs/commit/4ea285d34ca40c06bb2a589ba746561737b2833f))
- *(ci)* Scaffold the crate and the guards that must be seen red ([d42b5e2](https://github.com/murat-akpinar/k8rs/commit/d42b5e2aaac24eaebd56d39991a5c5611c7950e0)) — cargo init at 0.0.0, clippy.toml, deny.toml, justfile and the CI workflow. Two guards ship with their own proof: test-guard compares declared against listed tests, write-guard derives the write ban from kube's own Api surface.
- *(fixtures)* Sanitize before capture, and cover the two blind spots ([17d2556](https://github.com/murat-akpinar/k8rs/commit/17d2556c5e1ec36bf16e7792707ff8b583635b6e)) — The sanitizer lands before the first fixture, as required: payloads destroyed, references kept, and an object whose node identifiers are not the kind cluster's is refused rather than quietly rewritten. It is tested against a poisoned object in just check and in CI.
- *(fixtures)* Add certificate fixtures with pinned dates ([f2ad09d](https://github.com/murat-akpinar/k8rs/commit/f2ad09d455559a0ff3be690afd564ccaa846bf5d)) — Three self-signed client certificates for the C-series rules, generated locally with their keys deleted. The dates are pinned rather than relative: a fixture generated with -days 20 is a test that fails in three weeks, and the usual repair for that is to weaken the test. Snapshot carries now, so the test states the date it asks about.
- *(fixtures)* Capture the real cluster, and audit the bytes that land ([64016c2](https://github.com/murat-akpinar/k8rs/commit/64016c28c46bca5402d67a30533efd99bfcf6df8)) — The capture ran end to end against a three-node kind cluster (kindest/node:v1.36.1) on the LAN test host. cluster.sh verify passed 11/11 against settled state — every deliberately broken pod reached the state its rule is about — and 20 objects plus the version stamp were captured, each one through sanitize.jq on the way out.
- *(fixtures)* Re-capture, and add the pending CSR for C3 ([85d087b](https://github.com/murat-akpinar/k8rs/commit/85d087beae1144357f71798f199446b493e1bd89)) — verify passed 13/13 on the kind cluster, then the capture ran: 23 fixtures from v1.36.1. Two are new — restarts.json (rule 5) and healthy-replicasets.json, the negative side of W1 and W2, which has to be a ReplicaSet because the ReplicaFailure condition it must not have never appears anywhere else.
- *(rules)* Add the Finding struct and the identity it groups by ([3fe963b](https://github.com/murat-akpinar/k8rs/commit/3fe963b54adf069fac2af8424f4662c809ead337)) — The shared contract rules.rs, views.rs and the --once printer meet on: Severity, ObjectKind, ObjectId and Finding, with the one behaviour a compile cannot catch under test — declaration order is severity order.
- *(rules)* Add the snapshot types the rule engine reads ([a121979](https://github.com/murat-akpinar/k8rs/commit/a1219799c6c1248b09e9ca6df6510adb5eb6f317)) — PodSnapshot, NodeSnapshot, WorkloadSnapshot and ClusterSnapshot, with the single decode from the API types. k8s.rs fills them in Phase 5; this layer defines the contract, and it freezes at Phase 4 close.
- *(rules)* Put the current time on the snapshot the rules read ([aaecf7b](https://github.com/murat-akpinar/k8rs/commit/aaecf7babe1e82445e8a6de2c4bfcdb32308a4e6)) — `ClusterSnapshot` carries `now`, as `meta::v1::Time` — the same newtype every decoded API timestamp already wears, so the comparison rule 12 and C1 make is two values of one type. A rule reads the clock as a field or it is not pure, and a fixture that pins the moment cannot expire.
- *(rules)* Findings carry a timestamp and one age formatter ([3f0fabf](https://github.com/murat-akpinar/k8rs/commit/3f0fabf284471797745bc7d4b1001c6308da25fc)) — A finding now carries the moment its event happened, never a pre-formatted phrase, so both renderers ask `Finding::age(now)` for "4 min ago" and a test asserts a duration instead of parsing English back into a number. The rungs are the strings screens/ already prints; the shared function lives in rules.rs because ui.rs is Phase 11 and the --once printer is this phase, and two copies of a ladder is how two renderers start disagreeing about one finding.
- *(rules)* Diagnose the nine pod failures that need no second watch ([a3bd1fc](https://github.com/murat-akpinar/k8rs/commit/a3bd1fc33b32a52ea7967de6db577e8d8e0350de)) — CrashLoopBackOff, OOMKilled, image pull, missing ConfigMap or Secret, high restart counts, a failed previous run, running-but-out-of-the-Service, an escalated hostPath mount, and a pod that will not shut down — each a pure function over the snapshot, each with a fixture that fires it and healthy ones that must not.
- *(rules)* Explain a Pending pod in the scheduler's own words ([69e0afd](https://github.com/murat-akpinar/k8rs/commit/69e0afd7176eb178dd62de7e2230a30ca434374e)) — Rule 10 quotes the sentence kube-scheduler wrote onto the pod — which node counts refused it and why — instead of paraphrasing it, and frames the quote so a beginner can read a machine's output. No Events watch: the scheduler puts its reason on the object.
- *(rules)* See the init containers and sidecars the rules were blind to ([eb72646](https://github.com/murat-akpinar/k8rs/commit/eb726461cd21ce361ee36cd363f2bba151a4d626)) — A pod at Init:CrashLoopBackOff produced no finding at all, and neither did a crashlooping native sidecar — the rules read containerStatuses only, and both of those live in the other array. The card now says which kind of container it is, because "the app container is fine, the init one is not" is the diagnosis a beginner cannot reach alone.
- *(rules)* Catch the pod that was placed and never started ([84e5628](https://github.com/murat-akpinar/k8rs/commit/84e56280650627ba15b2b9025a5774010cf213f8)) — A pod given a node whose containers never start — a volume that will not mount, a sandbox with no network, a container the runtime cannot create — was invisible to every rule, and to rule 10 as well, because such a pod is scheduled. It fires as a residual: nothing else explains it, ten minutes after the scheduler placed it, WARN because the one healthy thing that still looks like this is a slow pull.
- *(rules)* Catch the pod that nothing has even looked at ([6a61636](https://github.com/murat-akpinar/k8rs/commit/6a61636a40aaad8bed3d283d967391efbe7a6733)) — A pod that is Pending with no PodScheduled condition at all has not been judged by anything: kube-scheduler is down, or spec.schedulerName names a scheduler that is not installed or lacks RBAC. Every other rule that answers why a pod is not running reads a container status or a condition, and such a pod has neither, so k8rs printed nothing is broken while the whole cluster sat Pending.
- *(rules)* Escalate on the directory a runtime socket sits under, not just the socket ([72d8d78](https://github.com/murat-akpinar/k8rs/commit/72d8d78902ff0a5f82151a0bf52dd7fa9683522f)) — The escalator learned a second spelling of a socket last time; the operator review asked the next question and it had a worse answer. is_runtime_socket matched the socket file and nothing above it, so a container handed the directory — /run/containerd, /var/run, /run itself — got exactly the same node-root and fell through to the writable branch.
- *(rules)* The node rules, and the six things a live cluster said about them ([a559019](https://github.com/murat-akpinar/k8rs/commit/a559019bc9f62dd8c79910badb45d89c503a6072)) — N1-N6 land together: a node that stopped answering, one cordoned with work still on it, one running low on disk or memory or process IDs, a kubelet too far behind the control plane, a node that promised more than it has, and the answer to why a Pending pod has nowhere to go. Three draw a node card, two are the Versions and Capacity reports' input, and the sixth is not a card at all.
- *(rules)* The workload rules W1 and W2 ([38f6f77](https://github.com/murat-akpinar/k8rs/commit/38f6f7730aac0961ff3c72e70a7a0b7cf3866182)) — W1 reads a ReplicaSet's ReplicaFailure condition and quotes the API server's refusal word for word; W2 reads a Deployment whose Progressing condition says the rollout gave up. Both matter because the pods they are about may not exist, so no pod rule can fire and the screen would otherwise be empty while nothing works. analyze() traverses snapshot.workloads for the first time.
- *(fixtures)* The capture trip — twelve shapes no committed object could reach ([1591e82](https://github.com/murat-akpinar/k8rs/commit/1591e82b38a9babbe736a7d4e4a17c21b410a14d)) — just fixtures ran against a live four-node kind cluster: 48 fixtures from kindest/node:v1.36.1, cluster.sh verify 37/37, and break proved idempotent over a cluster it had already broken.
- *(rules)* C1, the kubeconfig certificate that locks you out ([cccbad5](https://github.com/murat-akpinar/k8rs/commit/cccbad55fcb408d9ff034ff88c1aa788081944b9)) — The one finding with no API object behind it: it reads a file on the user's own machine and files under the kubeconfig context name, with no uid and no kubectl command, because none shows a certificate's expiry.

### 🐛 Bug Fixes

- *(ci)* Let cargo-deny accept our own GPL licence ([2717bd2](https://github.com/murat-akpinar/k8rs/commit/2717bd2b7cfd8cccd785111652ee12f407dfc9fe)) — cargo-deny checks the root crate too, so the permissive-only policy written for dependencies rejected k8rs itself. The exception is scoped to this crate; a copyleft dependency is still refused.
- *(fixtures)* Sanitize every shape the capture produces, not just one object ([842a767](https://github.com/murat-akpinar/k8rs/commit/842a767f95fac9bc1522a8000c74c9344fdd896b)) — Found by auditing Phase 2 before running the capture — nothing was failing.
- *(fixtures)* Strip addresses, and guard the pinned certificate dates ([90b8f66](https://github.com/murat-akpinar/k8rs/commit/90b8f6640411cf811044f392191229a646cda168)) — Three findings, all from checking claims against reality rather than reading them.
- *(fixtures)* Redact secrets that are not the whole string ([99a7a53](https://github.com/murat-akpinar/k8rs/commit/99a7a53bdebb9726bc9570966a0edfa7224cbb82)) — The IPv4 rule was anchored to the whole value, so two ordinary shapes walked past it: "10.244.0.0/24", an address wearing a suffix, and "dial tcp 172.18.0.1:53: no such host", an address quoted in the message kubelet writes when it cannot reach one. Both were sitting in the committed fixtures. The filter's own comment argued the gap away on the grounds that a foreign capture is refused anyway — but the refusal keys on node names, and this cluster's nodes are k8rs-* whatever address the apiserver was given, so a kind cluster on a real LAN passes it and takes that LAN address out inside a message.
- *(guards)* Read the signatures kube actually writes ([206d144](https://github.com/murat-akpinar/k8rs/commit/206d144d4b5b35b3cfc53b8b6d22b0b0c64170c4)) — write-guard derives its ban list by parsing every &self method of Api<K>, so under-extraction is silent: a method it cannot read is a method that never gets banned. Against real kube-client 1.1.0 the line-anchored regex found 28 of 45. The 17 it missed include every patch* method, delete_collection, exec and entry.
- *(screens)* Make the 80x24 claim true, and measurable ([b2c444c](https://github.com/murat-akpinar/k8rs/commit/b2c444c86e2fb79ab94b7ccf1056883fb48913a1)) — "Minimum terminal: 80x24" was a claim nobody had measured. Measuring it needs display columns, not bytes: a correct 70-column frame line is 76 bytes, so wc -c would have been wrong on all 31 mockups.
- *(guards)* Refuse the node names that were not in .nodeName ([44230d0](https://github.com/murat-akpinar/k8rs/commit/44230d00563e2545be726036c7cac4cc6c9b1e7e)) — The foreign-cluster refusal read two fields. A node name identifies infrastructure in four more, all ordinary: status.nominatedNodeName, spec.nodeSelector["kubernetes.io/hostname"], metadata.labels["kubernetes.io/hostname"] — which the committed nodes.json carries for all three nodes, so this is not a hypothetical field — and the values[] of a nodeAffinity matchExpressions entry. A production capture arriving through any of them was sanitized and written instead of refused. Each now has its own assertion, so a partial fix cannot pass.
- *(guards)* Assert the claims the guards were only asserting in prose ([61c5483](https://github.com/murat-akpinar/k8rs/commit/61c54839b609946cbbf1f2b97cc6bb2328e05b9e)) — certs-test.sh states that a real cluster's client certificate never enters this repository, and asserted nothing of the kind. A throwaway from make-certs.sh is self-signed; anything issued by a real CA is not, so subject == issuer is the difference worth catching. It also read only the three fixtures it knows by name, so a fourth certificate dropped into tests/fixtures/certs/ was tested by nothing at all.
- *(screens)* Show the log-tab keys the help screen promised ([ce1c11f](https://github.com/murat-akpinar/k8rs/commit/ce1c11f74af299937feb60110dbdbcedfb805f25)) — help.md opens with "the footer always shows the keys valid right now; ? shows all of them", and its own rules say only keys that exist in this build appear. Three did not: f follow and c container, both recorded in D12, and Shift-P previous, which was recorded nowhere at all — it lived in the detail.md footer and in no decision. A key that exists in a mockup and nowhere else is a key nobody agreed to.
- *(fixtures)* Refuse a certificate request that names a real requester ([e0d2e02](https://github.com/murat-akpinar/k8rs/commit/e0d2e02a577f7533e589f99404260b9027c9a0a4)) — sanitize.jq treats a CSR's .spec.username and .spec.groups the way it treats node names — refused, not rewritten — against the set the pinned kind cluster issues by itself, read off the live cluster rather than recalled. .spec.extra and .spec.uid take the payload treatment instead: the credential id is a fresh hash every capture, so there is nothing to allowlist.
- *(fixtures)* Refuse the node name in a Node ownerReference, and redact bracketed IPv6 ([7444c9a](https://github.com/murat-akpinar/k8rs/commit/7444c9a558db08b0a12e03ef4fabd61495d5d92c)) — Preparing to capture kube-system found two holes in a sanitizer that had only ever been fed pods built from broken.yaml. A node name lives in five places, not the four D31 enumerated: kubelet writes an ownerReference of kind Node onto every static pod, and a foreign pod carrying one and no .nodeName was sanitized and written instead of refused. IPv6 stays anchored — unanchored :: matches a Rust path — except inside brackets, where the URL form etcd and the apiserver put in their --listen-client-urls flags is unambiguous.
- *(rules)* Rule 1 reads how the previous run ended ([f0946fb](https://github.com/murat-akpinar/k8rs/commit/f0946fba263866de3863435ac6f0b4b24489f61a)) — It drew "Container keeps crashing" over exit 0 on a batch job that finished, and a CRITICAL "keeps crashing" whose own evidence line read "an ordinary shutdown and not an error" over exit 143. Rule 6 had exempted both codes since it was written; rule 1 never looked. Neither object could exist in this repository before the capture trip, so no test could fail and no review could see it.
- *(rules)* Rule 5 reads how the previous run ended ([402d6d3](https://github.com/murat-akpinar/k8rs/commit/402d6d31f27e750e46533793077f61dd4ed9fa48)) — The card said "it is serving now, but something keeps killing it" over every container past the restart band, including ones whose restarts are clean exits. Rule 5 now branches on `ending` like rule 1, with four arms that each own their claim, their action and their command.

### 💼 Other

- Close the last gaps between just check and CI ([c0582ce](https://github.com/murat-akpinar/k8rs/commit/c0582ce94593715ca4c0bd61e22d670db7a28baa))

### 🚜 Refactor

- *(rules)* Move the tests beside rules.rs instead of inside it ([0c52097](https://github.com/murat-akpinar/k8rs/commit/0c52097c8c2a9c8cf7fc6e179e57f250202067a2)) — src/rules.rs was 8913 lines and 6584 of them were `#[cfg(test)] mod tests` — the product file was a quarter of its own file. The tests now live in src/rules_tests.rs behind the one declaration `#[cfg(test)] #[path = "rules_tests.rs"] mod tests;`, and CLAUDE.md carries it as a convention rather than a one-off: every product file that grows tests splits the same way, so analysis.rs does it in Phase 4 without anyone deciding again.

### 📚 Documentation

- Record the git remote and the history restart ([244f9ce](https://github.com/murat-akpinar/k8rs/commit/244f9cec080ba07a956b703b676f9aba629a616a))
- Close the four holes that let a green build lie ([3fc8b6d](https://github.com/murat-akpinar/k8rs/commit/3fc8b6dee11cdfa2033e6a2bb66feeb7179ee01f))
- Cover init containers and ship rule 10 in v1 ([8add7fb](https://github.com/murat-akpinar/k8rs/commit/8add7fbfee62ca99d6c2230b70f9b523a4cc15a0))
- Add the phase-close ritual to the workflow ([8333b66](https://github.com/murat-akpinar/k8rs/commit/8333b668eeb3453db3fed0a1398e043fcd0b5bda)) — A phase closes with the todo boxes checked and true, the security gate run, docs and changelog synced, CI green — and a plain statement that the context should be cleared. Clearing is the user's command; nothing here can issue it.
- Say how to stand up the test cluster and capture fixtures yourself ([f2835f5](https://github.com/murat-akpinar/k8rs/commit/f2835f5cdde40fb4f701e2aa1588247236593b6a)) — Every fixture in tests/fixtures/ came from a cluster a contributor can recreate in one command, and nothing in docs/ said so. Adds the sequence, the six environment knobs and what each is for, and the one behaviour that surprises people: a capture from a cluster that is not kind is refused rather than sanitized, because rewriting node names would break the pod-to- node joins the node rules are built on.
- Record what the capture proved, and what it left open ([4f3a0dd](https://github.com/murat-akpinar/k8rs/commit/4f3a0dd047326d0a371db961a31b2f34db341e97)) — Phase 2's capture box is half ticked and says so: cluster-up, break, verify 13/13 and just fixtures have all run for real; just cluster-down has not, because the cluster is deliberately left standing so the reproduce-it-yourself instructions can be checked against it. That is also the one step that strips broken-stuck's finalizer, so it is the one that can still fail.
- Add the agent workflow, the ownership map and the second-pass rule ([a3c733f](https://github.com/murat-akpinar/k8rs/commit/a3c733f52b1761de37bade8cdb0a4fe7557e8f1c)) — Five subagents (dev-core, dev-ui, tui-designer, tester, k8s-admin) with the main session as project manager: every path in the repo has exactly one writer, review is a blocking gate held by someone other than the author, and the boxes needing a cluster or a credential are handed back to the user instead of being improvised around.
- Put the merge in the workflow instead of handing it back ([e842f73](https://github.com/murat-akpinar/k8rs/commit/e842f73d1e8371c73e5c032347b9b185ac81d7b9)) — Phase close item 7 is now the whole ritual and the only place it is described: push, PR, wait for every check to report, merge commit, delete the branch, return to main and pull. Standing authorisation — a green PR of the current phase is not handed back for someone else to click.
- Develop on one long-lived development branch (D32) ([2619f1c](https://github.com/murat-akpinar/k8rs/commit/2619f1ca7ef9b8417b5432c9138ad4c6f9b1e536)) — Replaces the branch-per-phase model, which was invented before anything was built on it and cost a create step, a delete step and a "which branch am I on" question per phase — while duplicating a boundary todo.md already enforces.
- Add the repository map, one row per path with its owner ([83c5fb7](https://github.com/murat-akpinar/k8rs/commit/83c5fb71bf254ba3e86de4c101b46aa55753266a)) — Where a file lives, what it answers, and who may write it — plus an "I want to ..." table routing a change to the file it belongs in. The ownership column is copied from CLAUDE.md, which stays authoritative; this is the routing view, not a second source of truth.
- *(docs)* Compress CLAUDE.md and move its stories into NOTES ([6c0e89b](https://github.com/murat-akpinar/k8rs/commit/6c0e89bc03e90b45cf0b2fd24c34106a3d14a173)) — The file is loaded in full every session and after every compaction: 682 lines and 6,548 words of standing cost before any work is read. Every invariant, security-gate box, ownership row and cycle step is kept; the prose that argued for them a second time is now D60.
- *(docs)* Close phase 2, and record the three claims its closing pass falsified ([95f2263](https://github.com/murat-akpinar/k8rs/commit/95f2263b124942deb39fcd4fc18fe89ecd77f2dc)) — The phase's own security gate passes on every item, and by more than it asks: the sanitizer is fed three shapes rather than two (single object, List, and the kube-system List the mirror-pod capture added) and four framings rather than three.
- *(docs)* Put the rules work first in phase 3, and the ci row beside it ([418565e](https://github.com/murat-akpinar/k8rs/commit/418565e74133421bb8ce732d02d692e100641256))
- *(rules)* Name which age function a renderer calls ([b8322b3](https://github.com/murat-akpinar/k8rs/commit/b8322b33ba49d1e384491bac5264d092b3257ed6)) — Two doc blocks pointed ui.rs and the --once printer at the free `age`, while `Finding::age` sixty lines below said it exists so neither of them does. The free function takes two arguments of one type, so the swapped call compiles, cannot panic, and paints every card "just now" — which is the reason `Finding::age` was added, and a reader following the opening paragraph reached straight past it.
- *(docs)* Count the spec fields instead of the kinds, and give the skew box its two owners ([4950bfe](https://github.com/murat-akpinar/k8rs/commit/4950bfe4bdb721b09d0bd9b27d26abc1326d41e8)) — Invariant 6 said the rule set reads spec on "all three kinds", which is the kind of number that reads as precise and is not: a DaemonSet answers `desired` from status.desiredNumberScheduled, so it is the one watched kind the old "metadata + status only" would have survived. It now names the five spec fields a rule actually reads, which is checkable where a count was a guess.
- *(docs)* Make step 7's second pass read the landed tree, not the diffs ([57d1380](https://github.com/murat-akpinar/k8rs/commit/57d138035eef15d16c82d1d8c4e37af1294de64c)) — Every pass before it sees one slice: each agent reads its own diff, the reviewer reads the behaviour, and nobody reads the file the next person opens. A review can even create the defect it could not have seen — the operator review demanded Finding::age, the fix added and documented it correctly, and two older paragraphs three lines up kept pointing renderers at the function it replaced, through a green build and the author's own second pass.
- *(rules)* Cut the comment blocks that re-argue what NOTES already decides ([cdc2a89](https://github.com/murat-akpinar/k8rs/commit/cdc2a89e195846ce1fbebf35842c2efbbfe17c2d)) — rules.rs carried 3467 comment lines against 6167 of code, and 62% of the product half was prose. Every D-number it cites already exists in NOTES.md, so the essays were a second copy of the reasoning with no owner keeping the two in step. Each block is now what the item is plus the decision it cites.
- *(rules)* Finish the comment cut, and close the four gaps CLAUDE.md was hiding ([ab44e73](https://github.com/murat-akpinar/k8rs/commit/ab44e73ca71a3ca5a8e9c3d0374832e8bc7fe70b)) — The product half of rules.rs still carried 1557 comment lines against 1180 of code after the first pass, and the largest blocks were second copies of NOTES entries nobody keeps in step with them: rule 10's 74 lines against D73, rule 13's 63 against D72, rule 14's 53 against D74. It is 1021 now — 46% against 57% — every block what the item is plus the decision it cites. No code changed: the non-comment lines of both rules.rs and main.rs are byte-identical to the previous commit, checked by diffing each file with its comments stripped.
- *(ui)* The cordon-card round, and the age ladder's one home ([d46c2ee](https://github.com/murat-akpinar/k8rs/commit/d46c2eec5bc738efc18890ec261574d2b855c7ea)) — The cordon card now has a real timeAdded to print, so it prints the ladder's ordinary string and nothing on it reasons from the age: the field dates the taint, a spec.taints rewrite re-stamps it, so the number can only ever be too small — a safe floor to sort by and a fatal thing to argue from.
- *(rules)* Record the W-series, the age ladder and the capture host ([fe00fbf](https://github.com/murat-akpinar/k8rs/commit/fe00fbf827e491257d45121d2c3402cd7781a3cb)) — D82 — the W-series, and the card that would have taught people to mute the tool. Its subject is the gate order: the author's own pass was green, the first review found three blockers, the second found a defect the first fix had created. Also the shortfall's three arms and why each is the only one that sees a shape, the suppression keying on a pod's Ready condition rather than doing_its_job, the three lookups that fail towards unknown rather than towards down, FailedDelete ruled out of v1, a counter that may not contradict the severity beside it, and an action that may not name a command the object in front of it cannot run.
- *(fixtures)* Record the capture trip, the host it needs, and rule 1's contradiction ([dc99d6d](https://github.com/murat-akpinar/k8rs/commit/dc99d6d177e884e504b73604e4fafedcad577ca2)) — D84 — a memory-starved capture host silently turns OOMKilled into Error. The entry carries the wrong conclusion as well as the right one: it first blamed the host kernel, having varied kernel and free memory together and observed only one of them.
- *(rules)* D86 and D87 — the MSRV C1 cost, and the two screens it belongs on ([a35b8e9](https://github.com/murat-akpinar/k8rs/commit/a35b8e953038c7c0ec359190719aad2e0d333116)) — D86 — C1's parser costs three minor Rust versions, and the alternative was an accepted vulnerability. The advisory and its fix are the same release: time 0.3.47 closes RUSTSEC-2026-0009 and is the first that needs Rust 1.88. What a reachability analysis would have said is recorded here rather than in deny.toml, so it informs the next reader without excusing anything.
- *(docs)* Close phase 2, and record what deferring its trip actually cost ([cc1cff3](https://github.com/murat-akpinar/k8rs/commit/cc1cff36a7d46ec10acc933dfde73bc25bbb44c4))
- *(rules)* The exit table gains its 0 row, and D85 records both rounds ([8e2f2f3](https://github.com/murat-akpinar/k8rs/commit/8e2f2f36c5490493f06bb508e914037eefe946a5)) — The v1 rule set's translation table had no entry for exit 0, so the code printed it bare under a title claiming a crash. Rule 1's row said one sentence where it now produces three, each scoped to the one run a snapshot holds.

### 🧪 Testing

- *(fixtures)* Prove the predicates that decide a fixture is trustworthy ([03a1deb](https://github.com/murat-akpinar/k8rs/commit/03a1deb1c12d23cee7d38418fe297d4c628ac929)) — cluster.sh verify holds one jq predicate per fixture, and each decides whether that fixture reached the state its rule is about. They had only ever run against a live cluster, where being wrong is invisible both ways: too loose passes a pod in the wrong state, too tight burns the 420s timeout and fails a correct one — and jq cannot tell a broken filter from an unmet condition either, since both exit non-zero.
- *(fixtures)* Add the broken pod that has an owner, and fix a predicate that named one instant of a loop ([27c8443](https://github.com/murat-akpinar/k8rs/commit/27c8443319f050e53b51f95d5daab1228adf55d9)) — broken-owned is a Deployment whose pod crashes, so one object carries both halves of the owner chain: pod -> ReplicaSet -> Deployment. Every other pod fixture is bare, leaving the grouping key tested only in its no-owner case.
- *(fixtures)* Add the shapes the first capture could not produce, and the node states behind them ([31e11c9](https://github.com/murat-akpinar/k8rs/commit/31e11c9d7c3e930df153b61e7a0d1ccb759f4400)) — Thirteen shapes land in the two manifests: a StatefulSet at all, a Deployment caught mid-rollout, a DaemonSet whose pods cannot start, a resize the node cannot fit, a pod-level limit the container never declared, a subPath and a second container on the hostPath mount, a termination message the kubelet actually wrote, and on the healthy side a read-only host mount, a native sidecar, a pod-level request and an init container with resources. `broken-pending` is respun on a nodeSelector so N6's pod side has a real object.
- *(fixtures)* Capture the shapes the manifests were written for, and repin now ([1effe7b](https://github.com/murat-akpinar/k8rs/commit/1effe7b5bcb0a855458616cc5faf840a50b81295)) — The second trip ran end to end against kindest/node:v1.36.1: break -> verify (23/23) -> just fixtures (34 fixtures, then break-nodes and 3/3 node predicates) -> unbreak -> cluster-down. Four Phase 2 boxes close with it, and the capture was read before the teardown so a missing shape could still have been re-taken against a live cluster.
- *(rules)* Retire the syntheses onto the captured objects ([a68044f](https://github.com/murat-akpinar/k8rs/commit/a68044ff7ce6882295fd19e05324933aa343a110)) — Twelve tests that planted a shape on a decoded copy now read a real capture: rule 2's recency clause on a serving container, rule 5's CRITICAL band and its serving half, the init loop that retried and succeeded, the unready sidecar, rule 14's positive, rule 13's storage branch, the Succeeded and Failed skips, rule 6's exit 0, 143 and 126/127 arms, the runtime-socket escalator, and From<StatefulSet>, whose doc had said no test could cover it yet.

### ⚙️ Miscellaneous Tasks

- Init ([542deb0](https://github.com/murat-akpinar/k8rs/commit/542deb041294e191d6af55acc0e1aef3b246cfa9)) — The whole design phase: decision record, requirements, screens, build plan and the working rules. No code yet.
- *(docs)* Pin a model to each subagent, by the weight of its job ([12aaab4](https://github.com/murat-akpinar/k8rs/commit/12aaab4d1e2fe30365f11af91a3b014846682091)) — The four agents that write product code, guards or the blocking review run on Opus; the screen designer, which writes ASCII and wording and no Rust, runs on Sonnet. Without the field every agent inherited whatever the main session happened to be set to, so the review that is meant to catch a lie could silently be the cheapest model in the house.
- *(ci)* Run the cross-compile matrix from just check ([5490d26](https://github.com/murat-akpinar/k8rs/commit/5490d26d5a4d1f63139de0cffdc1321df62363df)) — CI cross-compiled the four release targets and `just check` ran nothing equivalent, so a break that only appears for musl or darwin was discoverable only after a push — the exact failure "`just check` is the whole of CI, or it is a lie" exists to prevent, and cross-compilation breaks at link time and late.
