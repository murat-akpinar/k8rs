## [unreleased]

### 🚀 Features

- *(rules)* See workloads whose pods were never created ([4ea285d](https://github.com/murat-akpinar/k8rs/commit/4ea285d34ca40c06bb2a589ba746561737b2833f))
- *(ci)* Scaffold the crate and the guards that must be seen red ([d42b5e2](https://github.com/murat-akpinar/k8rs/commit/d42b5e2aaac24eaebd56d39991a5c5611c7950e0)) — cargo init at 0.0.0, clippy.toml, deny.toml, justfile and the CI workflow. Two guards ship with their own proof: test-guard compares declared against listed tests, write-guard derives the write ban from kube's own Api surface.
- *(fixtures)* Sanitize before capture, and cover the two blind spots ([17d2556](https://github.com/murat-akpinar/k8rs/commit/17d2556c5e1ec36bf16e7792707ff8b583635b6e)) — The sanitizer lands before the first fixture, as required: payloads destroyed, references kept, and an object whose node identifiers are not the kind cluster's is refused rather than quietly rewritten. It is tested against a poisoned object in just check and in CI.
- *(fixtures)* Add certificate fixtures with pinned dates ([f2ad09d](https://github.com/murat-akpinar/k8rs/commit/f2ad09d455559a0ff3be690afd564ccaa846bf5d)) — Three self-signed client certificates for the C-series rules, generated locally with their keys deleted. The dates are pinned rather than relative: a fixture generated with -days 20 is a test that fails in three weeks, and the usual repair for that is to weaken the test. Snapshot carries now, so the test states the date it asks about.
- *(fixtures)* Capture the real cluster, and audit the bytes that land ([64016c2](https://github.com/murat-akpinar/k8rs/commit/64016c28c46bca5402d67a30533efd99bfcf6df8)) — The capture ran end to end against a three-node kind cluster (kindest/node:v1.36.1) on the LAN test host. cluster.sh verify passed 11/11 against settled state — every deliberately broken pod reached the state its rule is about — and 20 objects plus the version stamp were captured, each one through sanitize.jq on the way out.
- *(fixtures)* Re-capture, and add the pending CSR for C3 ([85d087b](https://github.com/murat-akpinar/k8rs/commit/85d087beae1144357f71798f199446b493e1bd89)) — verify passed 13/13 on the kind cluster, then the capture ran: 23 fixtures from v1.36.1. Two are new — restarts.json (rule 5) and healthy-replicasets.json, the negative side of W1 and W2, which has to be a ReplicaSet because the ReplicaFailure condition it must not have never appears anywhere else.
- *(rules)* Add the Finding struct and the identity it groups by ([3fe963b](https://github.com/murat-akpinar/k8rs/commit/3fe963b54adf069fac2af8424f4662c809ead337)) — The shared contract rules.rs, views.rs and the --once printer meet on: Severity, ObjectKind, ObjectId and Finding, with the one behaviour a compile cannot catch under test — declaration order is severity order.
- *(rules)* Add the snapshot types the rule engine reads ([a121979](https://github.com/murat-akpinar/k8rs/commit/a1219799c6c1248b09e9ca6df6510adb5eb6f317)) — PodSnapshot, NodeSnapshot, WorkloadSnapshot and ClusterSnapshot, with the single decode from the API types. k8s.rs fills them in Phase 5; this layer defines the contract, and it freezes at Phase 4 close.
- *(rules)* Put the current time on the snapshot the rules read ([aaecf7b](https://github.com/murat-akpinar/k8rs/commit/aaecf7babe1e82445e8a6de2c4bfcdb32308a4e6)) — `ClusterSnapshot` carries `now`, as `meta::v1::Time` — the same newtype every decoded API timestamp already wears, so the comparison rule 12 and C1 make is two values of one type. A rule reads the clock as a field or it is not pure, and a fixture that pins the moment cannot expire.
- *(rules)* Findings carry a timestamp and one age formatter ([3f0fabf](https://github.com/murat-akpinar/k8rs/commit/3f0fabf284471797745bc7d4b1001c6308da25fc)) — A finding now carries the moment its event happened, never a pre-formatted phrase, so both renderers ask `Finding::age(now)` for "4 min ago" and a test asserts a duration instead of parsing English back into a number. The rungs are the strings screens/ already prints; the shared function lives in rules.rs because ui.rs is Phase 11 and the --once printer is this phase, and two copies of a ladder is how two renderers start disagreeing about one finding.
- *(rules)* Diagnose the nine pod failures that need no second watch ([a3bd1fc](https://github.com/murat-akpinar/k8rs/commit/a3bd1fc33b32a52ea7967de6db577e8d8e0350de)) — CrashLoopBackOff, OOMKilled, image pull, missing ConfigMap or Secret, high restart counts, a failed previous run, running-but-out-of-the-Service, an escalated hostPath mount, and a pod that will not shut down — each a pure function over the snapshot, each with a fixture that fires it and healthy ones that must not.
- *(rules)* Explain a Pending pod in the scheduler's own words ([69e0afd](https://github.com/murat-akpinar/k8rs/commit/69e0afd7176eb178dd62de7e2230a30ca434374e)) — Rule 10 quotes the sentence kube-scheduler wrote onto the pod — which node counts refused it and why — instead of paraphrasing it, and frames the quote so a beginner can read a machine's output. No Events watch: the scheduler puts its reason on the object.
- *(rules)* See the init containers and sidecars the rules were blind to ([eb72646](https://github.com/murat-akpinar/k8rs/commit/eb726461cd21ce361ee36cd363f2bba151a4d626)) — A pod at Init:CrashLoopBackOff produced no finding at all, and neither did a crashlooping native sidecar — the rules read containerStatuses only, and both of those live in the other array. The card now says which kind of container it is, because "the app container is fine, the init one is not" is the diagnosis a beginner cannot reach alone.
- *(rules)* Catch the pod that was placed and never started ([84e5628](https://github.com/murat-akpinar/k8rs/commit/84e56280650627ba15b2b9025a5774010cf213f8)) — A pod given a node whose containers never start — a volume that will not mount, a sandbox with no network, a container the runtime cannot create — was invisible to every rule, and to rule 10 as well, because such a pod is scheduled. It fires as a residual: nothing else explains it, ten minutes after the scheduler placed it, WARN because the one healthy thing that still looks like this is a slow pull.
- *(rules)* Catch the pod that nothing has even looked at ([6a61636](https://github.com/murat-akpinar/k8rs/commit/6a61636a40aaad8bed3d283d967391efbe7a6733)) — A pod that is Pending with no PodScheduled condition at all has not been judged by anything: kube-scheduler is down, or spec.schedulerName names a scheduler that is not installed or lacks RBAC. Every other rule that answers why a pod is not running reads a container status or a condition, and such a pod has neither, so k8rs printed nothing is broken while the whole cluster sat Pending.
- *(rules)* Escalate on the directory a runtime socket sits under, not just the socket ([72d8d78](https://github.com/murat-akpinar/k8rs/commit/72d8d78902ff0a5f82151a0bf52dd7fa9683522f)) — The escalator learned a second spelling of a socket last time; the operator review asked the next question and it had a worse answer. is_runtime_socket matched the socket file and nothing above it, so a container handed the directory — /run/containerd, /var/run, /run itself — got exactly the same node-root and fell through to the writable branch.
- *(rules)* The node rules, and the six things a live cluster said about them ([a559019](https://github.com/murat-akpinar/k8rs/commit/a559019bc9f62dd8c79910badb45d89c503a6072)) — N1-N6 land together: a node that stopped answering, one cordoned with work still on it, one running low on disk or memory or process IDs, a kubelet too far behind the control plane, a node that promised more than it has, and the answer to why a Pending pod has nowhere to go. Three draw a node card, two are the Versions and Capacity reports' input, and the sixth is not a card at all.
- *(rules)* The workload rules W1 and W2 ([38f6f77](https://github.com/murat-akpinar/k8rs/commit/38f6f7730aac0961ff3c72e70a7a0b7cf3866182)) — W1 reads a ReplicaSet's ReplicaFailure condition and quotes the API server's refusal word for word; W2 reads a Deployment whose Progressing condition says the rollout gave up. Both matter because the pods they are about may not exist, so no pod rule can fire and the screen would otherwise be empty while nothing works. analyze() traverses snapshot.workloads for the first time.
- *(fixtures)* The capture trip — twelve shapes no committed object could reach ([1591e82](https://github.com/murat-akpinar/k8rs/commit/1591e82b38a9babbe736a7d4e4a17c21b410a14d)) — just fixtures ran against a live four-node kind cluster: 48 fixtures from kindest/node:v1.36.1, cluster.sh verify 37/37, and break proved idempotent over a cluster it had already broken.
- *(rules)* C1, the kubeconfig certificate that locks you out ([cccbad5](https://github.com/murat-akpinar/k8rs/commit/cccbad55fcb408d9ff034ff88c1aa788081944b9)) — The one finding with no API object behind it: it reads a file on the user's own machine and files under the kubeconfig context name, with no uid and no kubectl command, because none shows a certificate's expiry.
- *(rules)* Rule 15 — a container that has stopped and nothing is starting it again ([0dc2f92](https://github.com/murat-akpinar/k8rs/commit/0dc2f92c015f8e9273b3f3e2b9497d62396d95ff)) — `kubectl get pods` prints `Error` for a pod at `1/3`, and every rule in this file was silent on it. The shape: a container terminated with a bad exit, `restartCount: 0`, inside a pod that is still `Running` because a sibling is. D96 refused the run a container is sitting in *as a transient* and named this as the one exception — it is permanent, and nothing else records it.
- *(fixtures)* Capture the four objects the clean-exit rules were written without ([50bc2ce](https://github.com/murat-akpinar/k8rs/commit/50bc2ce75f884c0cfb59ef67882f3274232a6932)) — A probe kill the program reported as exit 0 on a 32s run, a restart count raised by three node reboots, a container the kubelet restarts under restartPolicy Never, and D100's settled gang restart. All four are guarded by the field the rule reads, and the whole corpus was recaptured from a fresh kind v1.36.1 cluster.
- *(main)* The temporary driver prints findings from files, and the strip goes on the value entering the sentence ([56dcef3](https://github.com/murat-akpinar/k8rs/commit/56dcef350e13d6381211ca5f36c3dc5959e45987)) — Phase 3's last box. `main.rs` reads Kubernetes objects out of JSON files named on argv, runs `analyze` over them and draws screens/once.md's card, diverging in three named places: no owner grouping, severity as the whole sort key, and the Info band the console will not draw (NOTES D121).
- *(rules)* The report shape, where the variant says what the cursor may do ([bc0970f](https://github.com/murat-akpinar/k8rs/commit/bc0970f5e4ad4ef750b09df3cb95ddf8e595a5a4)) — Phase 4's shared contract, written alone because six report boxes fill it: a plain-language pane title, an optional sidebar badge, and a body of Row::Answer | Prose | NotComputed.
- *(rules)* Open the door the reports were always going to knock on ([533f123](https://github.com/murat-akpinar/k8rs/commit/533f12348c67a473563212a7a7a14148762f9110)) — Twelve items become pub(crate) under D129's bounds — not the thirteen proposed: charged is reached through promised, SUPPORTED_SKEW through kubelet_too_far_behind, and quantity_milli has no decided caller, so all three stay private. Every changed line outside the snapshot region is a visibility keyword.
- *(rules)* The six analysis reports, over a cluster snapshot ([1ef9e3f](https://github.com/murat-akpinar/k8rs/commit/1ef9e3fbfdc5dce694310781f3e820774f791fa7)) — Capacity, Drain safety, Waste, Posture, Versions and Certificates — Phase 4's Family C, pure functions taking the snapshot and the findings `analyze` already returned. NOTES § D134 holds the decisions.
- *(rules)* Read the declared restart rules, and name the pods a rollout is losing ([c1eafbb](https://github.com/murat-akpinar/k8rs/commit/c1eafbbb5284bb27df744fb3d43e3136f525bd55)) — Two Phase 4 boxes, one turn. Both are a snapshot field, a prune line and then a rule, inside the window D42 leaves open before the types freeze.
- *(rules)* A seventh report for the containers that keep restarting ([1c5a9d1](https://github.com/murat-akpinar/k8rs/commit/1c5a9d12da14bba94f88db6eba0826f81150d8e5)) — The hole D101 left visible: a container that is serving right now and keeps dying draws nothing from rules 1, 2, 5 and 6 between its restarts. It now has a pane of its own — one row per container, the count and the current run's age in two paragraphs, never divided, jumping to the pod because there is no finding to jump to.
- *(ci)* The client lands, and the write ban starts containing something ([e70e69d](https://github.com/murat-akpinar/k8rs/commit/e70e69d833307200bbfc88893c3406f1e2af6174)) — kube 4.2.0 and tokio 1.53.1 enter the build for Phase 5. 4.2.0 is the line that resolves against the k8s-openapi 0.28.0 pin — 3.1.0 wants ^0.27.0 — read off the crates.io index rather than guessed, and the two are upgraded together. Features are named rather than inherited: kube gets client, runtime and rustls-tls and nothing else, so oauth, oidc, socks5, ws and a second TLS backend stay out of a binary whose whole trust model is one kubeconfig and one API server. rustls is chosen by the four cross-compiled release targets, not by preference; the cost is that rustls parses certificates more strictly than OpenSSL, and D140 says so.
- *(k8s)* The watch, the prune that is the decode, and one task holding the store ([5141029](https://github.com/murat-akpinar/k8rs/commit/51410295e0df1a476322ee5614ef4b028f5a1299)) — Phase 5's first box. k8s.rs carries Store, Watch<T>, the bootstrap gate and the driver loop: five watches over Pods, Nodes, Deployments, StatefulSets and DaemonSets, merged with select_all into one task holding one &mut Store — no lock, no channel, and the only line naming a kind is the apply function handed to updates().
- *(k8s)* Strip and bound every field at ingest, and stop gluing two words together ([cd8929e](https://github.com/murat-akpinar/k8rs/commit/cd8929e240b8b50168d909d2b546d021ce00f5cd)) — The security gate has said "sizes are bounded" since Phase 1 and nothing below this phase implemented it. Both halves now land at one place, k8s::ingest, between the decode and the store — the same place the prune is, for the same reason: one line, so no downstream consumer has to remember.
- *(k8s)* Read what kube does with the initial LIST instead of assuming it ([0152cb2](https://github.com/murat-akpinar/k8rs/commit/0152cb23ef3c7c7f6d2c2bd94fcb779e6a3dec5f)) — The box asked for a decision about limit/continue and a page size, and said to read kube's source rather than assume. The reading is the deliverable: kube already paginates, so there was no pagination to write, and saying so is the honest outcome rather than writing code to look busy.
- *(k8s)* Say which watch is still listing, and write down what a throttle costs ([08c3da5](https://github.com/murat-akpinar/k8rs/commit/08c3da5ff4aa3a15b42565b5e0a963f62f4dee93)) — The box asked whether kube-rs rate-limits us. It does not, and the proof is mechanical rather than a grep: tower's limiter lives behind a Cargo feature kube-client does not enable, and cargo tree -e features -i tower finds no limit at all, so the module is not compiled into this binary. client-go's QPS/burst throttle was never ported.
- *(k8s)* Name the oldest cluster k8rs supports, and make a stuck first sync a state ([503e408](https://github.com/murat-akpinar/k8rs/commit/503e4087e4ceecb33610ae018b5e6d64a75d589a)) — The floor is Kubernetes 1.29, and it is derived rather than taken from the upstream three-minor convention. Nothing k8rs sends is refused by an older server: limit and continue are chunking, beta-on since 1.9; allowWatchBookmarks is stable at 1.17 and a server that lacks it ignores the parameter; and with ListSemantic::MostRecent the watcher sends no resourceVersion, no resourceVersionMatch and no selectors at all. Down to 1.19 — the oldest spec checked — nothing is rejected.
- *(k8s)* Resolve a pod's owner to the thing somebody deployed, by uid and never by suffix ([96ab294](https://github.com/murat-akpinar/k8rs/commit/96ab2946902c742859e5cdf3d3f53ed3bf6f42ec)) — A card must be headed web, not web-7d4f5c6b8. The ReplicaSet is fetched on demand, cached by uid and never watched, and the hash is never chopped — which is easy to say and hard to test, because chopping the suffix after an answer lands gets the name right every single time. So the assertion is on the uid, and the red run reads kind right, namespace right, name right, uid wrong: the Deployment's uid appears nowhere in the pod, nowhere in its ownerReference and nowhere in the ReplicaSet's name, only in the ReplicaSet's own ownerReferences.
- *(k8s)* Build the sidebar's kind list from discovery, and count what it costs ([ce5806f](https://github.com/murat-akpinar/k8rs/commit/ce5806fb6dfeb1acd0ed33ca1e8bcae581547410)) — Browsable carries four strings, a bool and the verbs, is built from (ApiResource, ApiCapabilities) so it is testable with no Client, is filtered on list alone, and goes through ingest so it is stripped and bounded like everything else. Every test input is an invented CRD: if the function could tell a built-in from a custom resource the tests could not see it, which is invariant 12 as an assertion rather than a claim.
- *(k8s)* Let the api server print the browser's columns, and measure what keeping them fresh costs ([21f85a9](https://github.com/murat-akpinar/k8rs/commit/21f85a9ae7b699f69d6be1c69c7307ea2f04f69f)) — The Table fetch is built here and made by connect(): a Fetch is a path and an Accept header, the 406 fallback is derived from the request that was refused, and the plain object list is read for what it is whether it arrives after a 406 or as the 200 the Accept header's own fallback negotiated. Cells are JSON values, not strings, because a Deployment's Table sends numbers. Every column comes off columnDefinitions, so a CRD draws with no line written for it.
- *(rules)* Tell a pod a node threw away from a job that ran to completion ([79198cc](https://github.com/murat-akpinar/k8rs/commit/79198cc65ed2257ae013d1aa15ae289845edf31f)) — `finished()` is `Succeeded | Failed` and a kubelet eviction is `Failed`, so Waste counted a pod a node killed with the completed Job pods and explained it with the Job sentence. It draws two rows now, partitioned by `status.reason` inside the gate that already existed, so they always sum to the count the one row drew.
- *(k8s)* Probe what else the cluster serves, from the discovery answer ([4825c57](https://github.com/murat-akpinar/k8rs/commit/4825c57a88f972597d0994be63cda33bf739b095)) — capabilities() reads the same run_aggregated() result the sidebar is built from and answers which of seven optional things the cluster serves — no round trip of its own.
- *(k8s)* Give every watch its own failure, and notice one that ends ([1583f91](https://github.com/murat-akpinar/k8rs/commit/1583f915f79dbaac66dadd67de8a2391e1091c00))

### 🐛 Bug Fixes

- *(ci)* Let cargo-deny accept our own GPL licence ([2717bd2](https://github.com/murat-akpinar/k8rs/commit/2717bd2b7cfd8cccd785111652ee12f407dfc9fe)) — cargo-deny checks the root crate too, so the permissive-only policy written for dependencies rejected k8rs itself. The exception is scoped to this crate; a copyleft dependency is still refused.
- *(fixtures)* Sanitize every shape the capture produces, not just one object ([842a767](https://github.com/murat-akpinar/k8rs/commit/842a767f95fac9bc1522a8000c74c9344fdd896b)) — Found by auditing Phase 2 before running the capture — nothing was failing.
- *(fixtures)* Strip addresses, and guard the pinned certificate dates ([90b8f66](https://github.com/murat-akpinar/k8rs/commit/90b8f6640411cf811044f392191229a646cda168)) — Three findings, all from checking claims against reality rather than reading them.
- *(fixtures)* Redact secrets that are not the whole string ([99a7a53](https://github.com/murat-akpinar/k8rs/commit/99a7a53bdebb9726bc9570966a0edfa7224cbb82)) — The IPv4 rule was anchored to the whole value, so two ordinary shapes walked past it: "10.244.0.0/24", an address wearing a suffix, and "dial tcp 172.18.0.1:53: no such host", an address quoted in the message kubelet writes when it cannot reach one. Both were sitting in the committed fixtures. The filter's own comment argued the gap away on the grounds that a foreign capture is refused anyway — but the refusal keys on node names, and this cluster's nodes are k8rs-* whatever address the apiserver was given, so a kind cluster on a real LAN passes it and takes that LAN address out inside a message.
- *(guards)* Read the signatures kube actually writes ([206d144](https://github.com/murat-akpinar/k8rs/commit/206d144d4b5b35b3cfc53b8b6d22b0b0c64170c4)) — write-guard derives its ban list by parsing every &self method of Api<K>, so under-extraction is silent: a method it cannot read is a method that never gets banned. Against real kube-client 1.1.0 the line-anchored regex found 28 of 45. The 17 it missed include every patch* method, delete_collection, exec and entry.
- *(screens)* Make the 80x24 claim true, and measurable ([b2c444c](https://github.com/murat-akpinar/k8rs/commit/b2c444c86e2fb79ab94b7ccf1056883fb48913a1)) — "Minimum terminal: 80x24" was a claim nobody had measured. Measuring it needs display columns, not bytes: a correct 70-column frame line is 76 bytes, so wc -c would have been wrong on all 31 mockups.
- *(guards)* Refuse the node names that were not in .nodeName ([44230d0](https://github.com/murat-akpinar/k8rs/commit/44230d00563e2545be726036c7cac4cc6c9b1e7e)) — The foreign-cluster refusal read two fields. A node name identifies infrastructure in four more, all ordinary: status.nominatedNodeName, spec.nodeSelector["kubernetes.io/hostname"], metadata.labels["kubernetes.io/hostname"] — which the committed nodes.json carries for all three nodes, so this is not a hypothetical field — and the values[] of a nodeAffinity matchExpressions entry. A production capture arriving through any of them was sanitized and written instead of refused. Each now has its own assertion, so a partial fix cannot pass.
- *(guards)* Assert the claims the guards were only asserting in prose ([61c5483](https://github.com/murat-akpinar/k8rs/commit/61c54839b609946cbbf1f2b97cc6bb2328e05b9e)) — certs-test.sh states that a real cluster's client certificate never enters this repository, and asserted nothing of the kind. A throwaway from make-certs.sh is self-signed; anything issued by a real CA is not, so subject == issuer is the difference worth catching. It also read only the three fixtures it knows by name, so a fourth certificate dropped into tests/fixtures/certs/ was tested by nothing at all.
- *(screens)* Show the log-tab keys the help screen promised ([ce1c11f](https://github.com/murat-akpinar/k8rs/commit/ce1c11f74af299937feb60110dbdbcedfb805f25)) — help.md opens with "the footer always shows the keys valid right now; ? shows all of them", and its own rules say only keys that exist in this build appear. Three did not: f follow and c container, both recorded in D12, and Shift-P previous, which was recorded nowhere at all — it lived in the detail.md footer and in no decision. A key that exists in a mockup and nowhere else is a key nobody agreed to.
- *(fixtures)* Refuse a certificate request that names a real requester ([e0d2e02](https://github.com/murat-akpinar/k8rs/commit/e0d2e02a577f7533e589f99404260b9027c9a0a4)) — sanitize.jq treats a CSR's .spec.username and .spec.groups the way it treats node names — refused, not rewritten — against the set the pinned kind cluster issues by itself, read off the live cluster rather than recalled. .spec.extra and .spec.uid take the payload treatment instead: the credential id is a fresh hash every capture, so there is nothing to allowlist.
- *(fixtures)* Refuse the node name in a Node ownerReference, and redact bracketed IPv6 ([7444c9a](https://github.com/murat-akpinar/k8rs/commit/7444c9a558db08b0a12e03ef4fabd61495d5d92c)) — Preparing to capture kube-system found two holes in a sanitizer that had only ever been fed pods built from broken.yaml. A node name lives in five places, not the four D31 enumerated: kubelet writes an ownerReference of kind Node onto every static pod, and a foreign pod carrying one and no .nodeName was sanitized and written instead of refused. IPv6 stays anchored — unanchored :: matches a Rust path — except inside brackets, where the URL form etcd and the apiserver put in their --listen-client-urls flags is unambiguous.
- *(rules)* Rule 1 reads how the previous run ended ([f0946fb](https://github.com/murat-akpinar/k8rs/commit/f0946fba263866de3863435ac6f0b4b24489f61a)) — It drew "Container keeps crashing" over exit 0 on a batch job that finished, and a CRITICAL "keeps crashing" whose own evidence line read "an ordinary shutdown and not an error" over exit 143. Rule 6 had exempted both codes since it was written; rule 1 never looked. Neither object could exist in this repository before the capture trip, so no test could fail and no review could see it.
- *(rules)* Rule 5 reads how the previous run ended ([402d6d3](https://github.com/murat-akpinar/k8rs/commit/402d6d31f27e750e46533793077f61dd4ed9fa48)) — The card said "it is serving now, but something keeps killing it" over every container past the restart band, including ones whose restarts are clean exits. Rule 5 now branches on `ending` like rule 1, with four arms that each own their claim, their action and their command.
- *(rules)* The clean exit gets its third door, and a command that shows it ([a2b8e0b](https://github.com/murat-akpinar/k8rs/commit/a2b8e0bc2cf0be88f50bdd241d294135aee8f596)) — Rule 1 closed on an exhaustive pair — the program is meant to finish, or it is quitting early — with no door for the commonest loop in a real cluster: a liveness probe fails, the kubelet asks the container to stop, the app traps SIGTERM and shuts down tidily, `exit 0`, repeat. The clause and the card's command could not be decided apart, because what separates that reading from the other two is in `describe` and not in `get -o yaml`.
- *(rules)* 137 is read from the object, not guessed from the number ([732c2da](https://github.com/murat-akpinar/k8rs/commit/732c2da94c1a29f8d2f698f057088250ba79ad65)) — The `137` line named a cause the object cannot support: "did not stop when it was asked to — a failing liveness probe, or a shutdown that hangs" is three claims deep, and it was printed by rules 1, 5 and 6 alike, so an init container was offered a probe `validateInitContainers` forbids it one row above an action saying Kubernetes allows none.
- *(rules)* The two 137 reasons are endings, so every rule reads them once ([5e979db](https://github.com/murat-akpinar/k8rs/commit/5e979dbd7ae98bd1eb82b71f6c2764977230fca9)) — Rules 1 and 5 printed the two readings `exit_meaning` learned and knew neither. On one object rule 5 offered "check the memory limit and the liveness probe" under an evidence line saying Kubernetes lost track of the container, and rule 1 sent the reader to `logs --previous` for a run whose `containerID` the record does not carry — a command the API refuses, not merely advice that does not help.
- *(k8s)* Pin k8s-openapi to the newest generated types ([6c7a20f](https://github.com/murat-akpinar/k8rs/commit/6c7a20f2d86216f42ad7c8cad98d81590a7034e0)) — The pin sat at v1_32, the oldest feature the crate offers, while every fixture is captured from kind v1.36.1 — so four versions of fields decoded to nothing and nothing said so. serde drops an unknown field, which reads exactly like a field the cluster never set: "found none" and "there were none" are the same silence, one layer under the guards that exist for it.
- *(rules)* Age the serving restart card out and stop sampling it mid-restart ([a870b89](https://github.com/murat-akpinar/k8rs/commit/a870b89bf96b83bad5a284b559484a4e5beba385)) — A container that used its restart rules and has served ever since carried a permanent WARN on a screen whose subject is what is broken now. The field that separates a settled restart from a live one was already decoded and rule 5 never read it: state.running.startedAt moves on every restart, while the lastState a gang restart synthesizes carries neither stamp.
- *(rules)* The second copy of a shared sentence says nothing, so it goes ([527c255](https://github.com/murat-akpinar/k8rs/commit/527c255e8bdb7373180ea81ede1edfa0e134e07b)) — Rules 5 and 6 draw the same four-line action on one container. The fold lives in analyze, is scoped to the container, keeps the more severe card, and drops a card only when every fact on it is already on the survivor.
- *(rules)* The fold keeps the diagnosis, and its key stops being impersonable ([76ec655](https://github.com/murat-akpinar/k8rs/commit/76ec6550980ad5109f9e727b40407fc1b60c7b14)) — The operator review of the landed fold found two defects and the fixes found a third. Rule 6 leaves on doing_its_job, so the pair the fold collapses is always the non-serving one — and rule 5 appended its ending clause only when serving, which left every fold with a bare count and the diagnosis on the one line the pane may cut. The clause is on both titles now.
- *(rules)* Read a terminated record for what it carries, not for who wrote it ([c4ba814](https://github.com/murat-akpinar/k8rs/commit/c4ba8141a2ad2ecfadcb37d31ce9922a0892dd09)) — lastState.terminated has three authors — the container, the kubelet and the container runtime — and rules.rs knew about one and a half of them. Phase 3's Family A is that one sentence, and an ephemeral kind v1.36.1 cluster produced three defects underneath it that 211 green tests could not see.
- *(rules)* A card's parts were budgeted separately and never added up ([4c065c1](https://github.com/murat-akpinar/k8rs/commit/4c065c13b4b4a0987ac77cbefc2b465043e4c369)) — screens/alerts.md said an action wrapping past five lines was a rules.rs defect and four actions had been over it for weeks. The box framed it as either the budget is wrong or the actions are. It was neither: the parts table permitted 1 + 2 + 3 + 5 = 11 while the summary capped a card at 10, and the two numbers had never been added up. The cap is 12 now, derived from the pane rather than observed, and the title gets a cap for the first time — because a rule writes the frame and exit_meaning writes the translation that finishes it, so 5 of the 36 reachable combinations measure four lines and only ending's pairings keep them off screen.
- *(rules)* A number glued to a plural noun is a format string showing through ([8bc9307](https://github.com/murat-akpinar/k8rs/commit/8bc93075b1d27cdacbe6b4f276e0c62563b4d8be)) — `1 restarts` was spelled by hand in three places — rules 1 and 2 carried the same copied `format!("{} restarts", …)`, and rule 5's title carried a third wording of the same defect. `counted` has existed since the age ladder was written; the sites that did not call it predate the habit. All three go through it now.
- *(ci)* The shard index is zero-based and the run that says otherwise prints no MISSED line ([98538db](https://github.com/murat-akpinar/k8rs/commit/98538db926fbdc72a1676ecd51dcdd262da2eeb0)) — D118 landed with `--shard k/4` and the four shards written as 1/4…4/4. The fourth is not a shard: cargo-mutants refuses it with *shard k must be less than n*, so the real four are 0/4 1/4 2/4 3/4.
- *(rules)* Read the last run on record from the container, not from a field ([5f25442](https://github.com/murat-akpinar/k8rs/commit/5f254427346fab475b4880fff29424303d9bab49)) — `lastState.terminated` is the last run only while the container has moved on from it. Sitting in `state.terminated` it is the second to last, so rule 6 drew "The last run on record failed — exit 3" about neverrules.json's `retry`, which is stopped at exit 1, and handed a `--previous` that resolves through `lastState.terminated.containerID` and fetches that same wrong run.
- *(ci)* Stop the mutation gate filing untested mutants as unviable ([eccedd5](https://github.com/murat-akpinar/k8rs/commit/eccedd5a39d6727383aa8be9b22d9e1d6dd4adfe)) — `justfile` exports `RUSTFLAGS = "-D warnings"`, which reached `cargo mutants`. A mutant's replaced body leaves its parameters unused, `-D warnings` made that a build failure, and cargo-mutants files any build failure as `unviable` — so the gate reported a pass on mutants it never built.
- *(rules)* A budget whose first sync failed blocks the drain instead of asking for a moment ([6ccb7db](https://github.com/murat-akpinar/k8rs/commit/6ccb7dbdac7b6515f1655f57355d6df8db3b3dd1)) — drain_row asked has_not_caught_up before blocks_a_drain, so a PodDisruptionBudget whose status.observedGeneration was behind its generation could never reach the SyncFailed branch. Upstream's failSafe sets DisruptionsAllowed = 0 and the SyncFailed condition and does not advance status.observedGeneration, while eviction.go refuses every eviction while the status is behind — so a budget whose first sync failed sits at generation 1 / observedGeneration 0 forever, every eviction of its pods is refused, and the pane drew its quietest row: no band, sorted with the ready nodes, "needs a moment before it can be checked".
- *(ci)* A write does not have to go through Api, and the ban now knows it ([25ad02f](https://github.com/murat-akpinar/k8rs/commit/25ad02f9dc810bad722c78302d10705e39aa0192)) — The derived ban list came from impl Api<K>, and a write need not touch Api<K> at all: kube::core::Request builds a complete DELETE that Client::request sends, and it drew zero disallowed-method warnings. Not a regression — the grep deleted in the last commit never caught it either, and it has been open since the guard was written. The derivation now covers impl Request as well, and clippy.toml goes from 29 entries to 45.
- *(rules)* Let rule 13 see the pod no kubelet ever reported on ([dfe6136](https://github.com/murat-akpinar/k8rs/commit/dfe6136cfc680fb76134c3f82a94da3e3aa0c91f))
- *(ci)* Teach the token-hygiene scan the shapes it could not see ([64f8519](https://github.com/murat-akpinar/k8rs/commit/64f8519e00080c23b7c899fab0c1c235d1dfa55e)) — The check decided "can this type hold a credential" with a struct-only regex against a field type spelled Client, so an enum variant payload, a type alias and ClientBuilder were all invisible - and it could not name a foreign type at all. Both are Phase 5 boxes and both landed before connect(), because a guard that goes vacuous exactly when the credential arrives is what D141 already cost this project once.

### 💼 Other

- Close the last gaps between just check and CI ([c0582ce](https://github.com/murat-akpinar/k8rs/commit/c0582ce94593715ca4c0bd61e22d670db7a28baa))

### 🚜 Refactor

- *(rules)* Move the tests beside rules.rs instead of inside it ([0c52097](https://github.com/murat-akpinar/k8rs/commit/0c52097c8c2a9c8cf7fc6e179e57f250202067a2)) — src/rules.rs was 8913 lines and 6584 of them were `#[cfg(test)] mod tests` — the product file was a quarter of its own file. The tests now live in src/rules_tests.rs behind the one declaration `#[cfg(test)] #[path = "rules_tests.rs"] mod tests;`, and CLAUDE.md carries it as a convention rather than a one-off: every product file that grows tests splits the same way, so analysis.rs does it in Phase 4 without anyone deciding again.
- *(rules)* Split the rule tests by family, and say so where it is documented ([4d73366](https://github.com/murat-akpinar/k8rs/commit/4d73366f28cf66958354f4c45361ae8f949fe8b4)) — 17460 lines in one file was the largest single read in the repo and every agent turn paged through it. rules_tests.rs keeps the imports and the helpers more than one region reads; five #[path] modules hold the tests, one per region of rules.rs. No mod.rs, no lib.rs, and rules.rs is byte-identical — the product file does not split.

### 📚 Documentation

- Record the git remote and the history restart ([244f9ce](https://github.com/murat-akpinar/k8rs/commit/244f9cec080ba07a956b703b676f9aba629a616a))
- Close the four holes that let a green build lie ([3fc8b6d](https://github.com/murat-akpinar/k8rs/commit/3fc8b6dee11cdfa2033e6a2bb66feeb7179ee01f))
- Cover init containers and ship rule 10 in v1 ([8add7fb](https://github.com/murat-akpinar/k8rs/commit/8add7fbfee62ca99d6c2230b70f9b523a4cc15a0))
- Add the phase-close ritual to the workflow ([8333b66](https://github.com/murat-akpinar/k8rs/commit/8333b668eeb3453db3fed0a1398e043fcd0b5bda)) — A phase closes with the todo boxes checked and true, the security gate run, docs and changelog synced, CI green — and a plain statement that the context should be cleared. Clearing is the user's command; nothing here can issue it.
- Say how to stand up the test cluster and capture fixtures yourself ([f2835f5](https://github.com/murat-akpinar/k8rs/commit/f2835f5cdde40fb4f701e2aa1588247236593b6a)) — Every fixture in tests/fixtures/ came from a cluster a contributor can recreate in one command, and nothing in docs/ said so. Adds the sequence, the six environment knobs and what each is for, and the one behaviour that surprises people: a capture from a cluster that is not kind is refused rather than sanitized, because rewriting node names would break the pod-to- node joins the node rules are built on.
- Record what the capture proved, and what it left open ([4f3a0dd](https://github.com/murat-akpinar/k8rs/commit/4f3a0dd047326d0a371db961a31b2f34db341e97)) — Phase 2's capture box is half ticked and says so: cluster-up, break, verify 13/13 and just fixtures have all run for real; just cluster-down has not, because the cluster is deliberately left standing so the reproduce-it-yourself instructions can be checked against it. That is also the one step that strips broken-stuck's finalizer, so it is the one that can still fail.
- Add the agent workflow, the ownership map and the second-pass rule ([a3c733f](https://github.com/murat-akpinar/k8rs/commit/a3c733f52b1761de37bade8cdb0a4fe7557e8f1c)) — Five subagents (dev-core, dev-ui, tui-designer, tester, k8s-admin) with the main session as project manager: every path in the repo has exactly one writer, review is a blocking gate held by someone other than the author, and the boxes needing a cluster or a credential are handed back to the user instead of being improvised around.
- Put the merge in the workflow instead of handing it back ([e842f73](https://github.com/murat-akpinar/k8rs/commit/e842f73d1e8371c73e5c032347b9b185ac81d7b9)) — Phase close item 7 is now the whole ritual and the only place it is described: push, PR, wait for every check to report, merge commit, delete the branch, return to main and pull. Standing authorisation — a green PR of the current phase is not handed back for someone else to click.
- Develop on one long-lived development branch (D32) ([2619f1c](https://github.com/murat-akpinar/k8rs/commit/2619f1ca7ef9b8417b5432c9138ad4c6f9b1e536)) — Replaces the branch-per-phase model, which was invented before anything was built on it and cost a create step, a delete step and a "which branch am I on" question per phase — while duplicating a boundary todo.md already enforces.
- Add the repository map, one row per path with its owner ([83c5fb7](https://github.com/murat-akpinar/k8rs/commit/83c5fb71bf254ba3e86de4c101b46aa55753266a)) — Where a file lives, what it answers, and who may write it — plus an "I want to ..." table routing a change to the file it belongs in. The ownership column is copied from CLAUDE.md, which stays authoritative; this is the routing view, not a second source of truth.
- *(docs)* Compress CLAUDE.md and move its stories into NOTES ([6c0e89b](https://github.com/murat-akpinar/k8rs/commit/6c0e89bc03e90b45cf0b2fd24c34106a3d14a173)) — The file is loaded in full every session and after every compaction: 682 lines and 6,548 words of standing cost before any work is read. Every invariant, security-gate box, ownership row and cycle step is kept; the prose that argued for them a second time is now D60.
- *(docs)* Close phase 2, and record the three claims its closing pass falsified ([95f2263](https://github.com/murat-akpinar/k8rs/commit/95f2263b124942deb39fcd4fc18fe89ecd77f2dc)) — The phase's own security gate passes on every item, and by more than it asks: the sanitizer is fed three shapes rather than two (single object, List, and the kube-system List the mirror-pod capture added) and four framings rather than three.
- *(docs)* Put the rules work first in phase 3, and the ci row beside it ([418565e](https://github.com/murat-akpinar/k8rs/commit/418565e74133421bb8ce732d02d692e100641256))
- *(rules)* Name which age function a renderer calls ([b8322b3](https://github.com/murat-akpinar/k8rs/commit/b8322b33ba49d1e384491bac5264d092b3257ed6)) — Two doc blocks pointed ui.rs and the --once printer at the free `age`, while `Finding::age` sixty lines below said it exists so neither of them does. The free function takes two arguments of one type, so the swapped call compiles, cannot panic, and paints every card "just now" — which is the reason `Finding::age` was added, and a reader following the opening paragraph reached straight past it.
- *(docs)* Count the spec fields instead of the kinds, and give the skew box its two owners ([4950bfe](https://github.com/murat-akpinar/k8rs/commit/4950bfe4bdb721b09d0bd9b27d26abc1326d41e8)) — Invariant 6 said the rule set reads spec on "all three kinds", which is the kind of number that reads as precise and is not: a DaemonSet answers `desired` from status.desiredNumberScheduled, so it is the one watched kind the old "metadata + status only" would have survived. It now names the five spec fields a rule actually reads, which is checkable where a count was a guess.
- *(docs)* Make step 7's second pass read the landed tree, not the diffs ([57d1380](https://github.com/murat-akpinar/k8rs/commit/57d138035eef15d16c82d1d8c4e37af1294de64c)) — Every pass before it sees one slice: each agent reads its own diff, the reviewer reads the behaviour, and nobody reads the file the next person opens. A review can even create the defect it could not have seen — the operator review demanded Finding::age, the fix added and documented it correctly, and two older paragraphs three lines up kept pointing renderers at the function it replaced, through a green build and the author's own second pass.
- *(rules)* Cut the comment blocks that re-argue what NOTES already decides ([cdc2a89](https://github.com/murat-akpinar/k8rs/commit/cdc2a89e195846ce1fbebf35842c2efbbfe17c2d)) — rules.rs carried 3467 comment lines against 6167 of code, and 62% of the product half was prose. Every D-number it cites already exists in NOTES.md, so the essays were a second copy of the reasoning with no owner keeping the two in step. Each block is now what the item is plus the decision it cites.
- *(rules)* Finish the comment cut, and close the four gaps CLAUDE.md was hiding ([ab44e73](https://github.com/murat-akpinar/k8rs/commit/ab44e73ca71a3ca5a8e9c3d0374832e8bc7fe70b)) — The product half of rules.rs still carried 1557 comment lines against 1180 of code after the first pass, and the largest blocks were second copies of NOTES entries nobody keeps in step with them: rule 10's 74 lines against D73, rule 13's 63 against D72, rule 14's 53 against D74. It is 1021 now — 46% against 57% — every block what the item is plus the decision it cites. No code changed: the non-comment lines of both rules.rs and main.rs are byte-identical to the previous commit, checked by diffing each file with its comments stripped.
- *(ui)* The cordon-card round, and the age ladder's one home ([d46c2ee](https://github.com/murat-akpinar/k8rs/commit/d46c2eec5bc738efc18890ec261574d2b855c7ea)) — The cordon card now has a real timeAdded to print, so it prints the ladder's ordinary string and nothing on it reasons from the age: the field dates the taint, a spec.taints rewrite re-stamps it, so the number can only ever be too small — a safe floor to sort by and a fatal thing to argue from.
- *(rules)* Record the W-series, the age ladder and the capture host ([fe00fbf](https://github.com/murat-akpinar/k8rs/commit/fe00fbf827e491257d45121d2c3402cd7781a3cb)) — D82 — the W-series, and the card that would have taught people to mute the tool. Its subject is the gate order: the author's own pass was green, the first review found three blockers, the second found a defect the first fix had created. Also the shortfall's three arms and why each is the only one that sees a shape, the suppression keying on a pod's Ready condition rather than doing_its_job, the three lookups that fail towards unknown rather than towards down, FailedDelete ruled out of v1, a counter that may not contradict the severity beside it, and an action that may not name a command the object in front of it cannot run.
- *(fixtures)* Record the capture trip, the host it needs, and rule 1's contradiction ([dc99d6d](https://github.com/murat-akpinar/k8rs/commit/dc99d6d177e884e504b73604e4fafedcad577ca2)) — D84 — a memory-starved capture host silently turns OOMKilled into Error. The entry carries the wrong conclusion as well as the right one: it first blamed the host kernel, having varied kernel and free memory together and observed only one of them.
- *(rules)* D86 and D87 — the MSRV C1 cost, and the two screens it belongs on ([a35b8e9](https://github.com/murat-akpinar/k8rs/commit/a35b8e953038c7c0ec359190719aad2e0d333116)) — D86 — C1's parser costs three minor Rust versions, and the alternative was an accepted vulnerability. The advisory and its fix are the same release: time 0.3.47 closes RUSTSEC-2026-0009 and is the first that needs Rust 1.88. What a reachability analysis would have said is recorded here rather than in deny.toml, so it informs the next reader without excusing anything.
- *(docs)* Close phase 2, and record what deferring its trip actually cost ([cc1cff3](https://github.com/murat-akpinar/k8rs/commit/cc1cff36a7d46ec10acc933dfde73bc25bbb44c4))
- *(rules)* The exit table gains its 0 row, and D85 records both rounds ([8e2f2f3](https://github.com/murat-akpinar/k8rs/commit/8e2f2f36c5490493f06bb508e914037eefe946a5)) — The v1 rule set's translation table had no entry for exit 0, so the code printed it bare under a title claiming a crash. Rule 1's row said one sentence where it now produces three, each scoped to the one run a snapshot holds.
- *(docs)* Add the k9s prior-art review, read as a defect catalogue ([25450be](https://github.com/murat-akpinar/k8rs/commit/25450be1a4a2068a2a23be593f41457a96dc6cbd))
- *(docs)* Turn the twelve prior-art gaps into boxes ([83306f4](https://github.com/murat-akpinar/k8rs/commit/83306f46fafa5fcef05734a66510a21ad6656580))
- *(docs)* The tests split by rule family at phase close, the product file does not ([f644828](https://github.com/murat-akpinar/k8rs/commit/f644828c09675c92a973c4812464e1711e7829e5))
- *(docs)* Each phase names the prior-art classes it can ship, and the operator review checks them ([5ced22d](https://github.com/murat-akpinar/k8rs/commit/5ced22d0785a292468099efdeff9a324f16d0d15))
- *(docs)* The cluster splits by what a run produces, not by who runs it ([3de4b81](https://github.com/murat-akpinar/k8rs/commit/3de4b81903e778bf0dc1db9899feeb759ba5e483)) — k8s-admin's brief said "prefer checking" against the kind cluster while CLAUDE.md said the agents do not have one. The reviewer followed its brief; the contradiction was never asked. Rule by what the rule protected — fabrication, not access:
- *(rules)* The long-cycle restarter is a report row, not a fifth threshold ([b7fda0a](https://github.com/murat-akpinar/k8rs/commit/b7fda0a2b51cbdfb88c90018022df9ed19293b0b)) — D101 answers the box D100 left open: no clause over one Pod sample separates a settled container from one on a thirty-minute cycle, so rules 1, 2, 5 and 6 keep their suppressors unchanged and the count becomes a per-container row in the Phase 4 reports. Three candidates are refused in writing, the Events sequence is refused on the watch budget rather than on physics, and the costs are named — Alerts is silent between restarts, the card can vanish while it is being read, and --once carries no reports.
- *(docs)* The repo grows every box and nothing ever made it smaller ([fb5cd1c](https://github.com/murat-akpinar/k8rs/commit/fb5cd1c89ce47f9bcfb543efaaf83daa3e71a775))
- *(docs)* Trim the closed boxes in the earlier phases too, and box the guard gap ([c29fdfc](https://github.com/murat-akpinar/k8rs/commit/c29fdfce362efb52f2df5c879d55d9756b021c59)) — The trim was applied to Phase 3 only; the rule is not phase-scoped. Closed boxes now average 13 lines against 45. Two are left whole on purpose -- their bodies are a restart-semantics spec and a capture recipe, not the round-by-round history the rule removes.
- *(docs)* A phase closes on a run, a family review and a bounded triage ([0f2cf24](https://github.com/murat-akpinar/k8rs/commit/0f2cf24c0f43ce2082cd4cb694e54bd23080bfd3)) — The gate that closed a phase was `cargo test` over fixtures, so a phase could close having never been built or run anywhere. It now builds and runs on the test host and pastes what it printed, the code review runs after that rather than before it, and the loop back has a bound: a blocker re-runs what the fix could break, everything else is boxed in a later phase and the phase still closes.
- *(docs)* The mutation re-run moves to a tool, and a family is one turn ([b90f77a](https://github.com/murat-akpinar/k8rs/commit/b90f77a516e309537fd3cc2247f17e83fccfc684)) — Measured over one box: step 4's hand re-run of the author's own mutations took fourteen minutes and found nothing, while the family review found both real defects. So `just mutants` checks the claim that a test can fail, the author keeps pasting its own red and green, and `tester` keeps the work that found things. Boxes of one family are briefed, written and reviewed together, which is what the reviewer already reads them as.
- *(docs)* The agent files were carrying a copy of the rule that changed ([540b87e](https://github.com/murat-akpinar/k8rs/commit/540b87e46a5af76d42121e2edd1d40842dddbded)) — Three of them contradicted CLAUDE.md within the hour: tester was still told to re-run the author's mutations by hand, and both devs were told to pick their own box out of todo.md, which is the PM's step 1 and now spans a family. The cycle table said the same stale thing the section under it had already replaced. Each file keeps what is its own and links the rest, so the next rule change has one place to land.
- *(docs)* The mutation gate is scoped to the diff, because the file is 519 mutants ([c758100](https://github.com/murat-akpinar/k8rs/commit/c758100dc7d8949488c490d5e5287551e28ea9d1)) — Written this morning as `just mutants` over the changed file, and running it once showed that is hours per box: 519 mutants on rules.rs after a 58s baseline. Per box it is `cargo mutants --in-diff` over the box's own diff; the whole-file run stays the phase-close gate todo.md always had. Also records what is left: rules.rs is 46% doc comments, which is D103's second-copy defect one layer down.
- *(docs)* The scope argument was right and the number under it was not ([ee39aed](https://github.com/murat-akpinar/k8rs/commit/ee39aed83b58ca10b09d89ebb17c96406a2047b9))
- *(docs)* The workflow files say one thing about step 4, and the tests moved ([ad7cedc](https://github.com/murat-akpinar/k8rs/commit/ad7cedc68e7f7f7a4272e0beb75c7ac9a7da2b86)) — Three leftovers from the day's two process changes: the rule tests are a directory now and two paragraphs still called them a file, the mutation gate was still written whole-file in both agent files after it was scoped to the diff, and step 4's row named a tool where every other row names who runs it. Adds the leak the day actually produced — a rule resting on a number that was estimated instead of run.
- *(docs)* Restore the Phase 3 heading the compaction removed ([a9e9397](https://github.com/murat-akpinar/k8rs/commit/a9e939744f245a6779c63d36df5be8f49a985afb)) — Compacting todo.md took the `## Phase 3` heading with it, along with the phase goal and Phase 2's own done-when and frozen-after — which carries the justfile exception Phase 7 depends on. Everything under them survived, so the loss was invisible box by box: the file simply read as though Phase 2 ran to line 1546 and the rule "the first unchecked box in the lowest open phase" pointed into a closed phase. Restored from 3ba77ec verbatim.
- *(docs)* Phase 3's open boxes are two families, and six are not phase 3's ([64bc846](https://github.com/murat-akpinar/k8rs/commit/64bc8464863c2f0e0a386036b9f9d9edeac83742)) — D103 stopped new boxes being injected into a running phase but never triaged the pile already there, and Phase 3 has not closed since. All twenty-three open boxes read whole: almost none is deferrable on merit, but they are two classes, and a class is what D103 ruled is reviewed as one turn.
- *(docs)* Work with no phase gets a file, and measurements get a directory ([1b2cf66](https://github.com/murat-akpinar/k8rs/commit/1b2cf66263f421c9bee4a27ec83634623d8d4c3d)) — Two gaps, both from rules that were right. A box is never added to an open phase and what is found mid-flight is boxed in a later one — which makes the next phase's scope whatever the previous one tripped over: D106 moved six boxes and took Phase 4 from twelve open to eighteen. backlog.md is where an unphased finding goes instead, and nothing in it is work: no agent picks from it, /basla still reads todo.md and only todo.md.
- *(docs)* The family is the unit, and the invariants stop being copied ([43347e8](https://github.com/murat-akpinar/k8rs/commit/43347e868202afb72928e1ea2ad3a157ce19b75f)) — The user asked for a phase-based cycle because a box takes hours. The reason is real; the fix was not, so the family becomes the unit of the cycle instead of an exception inside it — D106 already turned Phase 3's twenty-three open boxes into six turns on the same day. The commit does not batch with it: a commit costs no time, so moving it to phase close buys nothing against hours per box and pays with the recovery point.
- *(docs)* The brief names the regions, and the md files were not the cost ([95bd54a](https://github.com/murat-akpinar/k8rs/commit/95bd54a1a66b3ee5c07a07e4a27f9593f1b714ad)) — A box cost two hours and the markdown files were named as the cause. Measured instead: one cold dev-core dispatch faces pod.rs at 9809 lines, NOTES at 8658, rules.rs at 5186 — and CLAUDE.md at 680, two percent of it. Compressing that file by three percent moved three parts in ten thousand, and an hour went there before anything was measured, which is the error D107 itself records.
- *(docs)* Nothing counts panics, and the guard that owes is named ([0416a6e](https://github.com/murat-akpinar/k8rs/commit/0416a6e0f28f5feb9539596ac28165fbec61594f))
- *(docs)* The crash_looping mutant was called the only one and the whole-file run says nineteen ([cd5db3f](https://github.com/murat-akpinar/k8rs/commit/cd5db3fb396f16ba43b5013260ef1e5a52129f75))
- *(docs)* The prune bounds memory and not first paint ([7ef4e57](https://github.com/murat-akpinar/k8rs/commit/7ef4e57429dc65ad86e629a2a5d9ddaeee5e2517)) — Invariant 6's prune line has been corrected twice about which fields survive it (D69, D97) and neither correction noticed what sits underneath: there is no way to ask the API server for a subset of status, so the prune runs after the decode and decides the resident set and nothing else. It serves the 50MB target and contributes nothing to first paint.
- *(docs)* The cluster you land on was inherited from whatever kubectl ran last ([3b98c1a](https://github.com/murat-akpinar/k8rs/commit/3b98c1a6bb52a709e3803c47e9bfebe424ec90db)) — D16 gave the switch a key and left the launch path alone: `k8rs` read `current-context` and connected in silence, so the answer to "which cluster am I about to touch" was eleven dim characters in a header nobody reads until after they have acted on it. D116 moves the same picker earlier — no second screen, no second key map, `connect()` is already re-callable — and only when there is a real choice: two or more contexts and no `--context`, current preselected, so `zero configuration on first run` stays literally true.
- *(docs)* The phase 3 close cross-family review, and the seven findings it left behind ([d804a32](https://github.com/murat-akpinar/k8rs/commit/d804a322ea4d0d9216cfd474031b105d6a47c2c6)) — The read no per-family review could do: the five regions of rules.rs together with the helpers they share, plus the driver that now prints them, plus 29 cards read as one screen. One `K8RS_CLUSTER=review` cluster, up and down inside the run, for the node-down and init-mount measurements.
- *(docs)* Phase 3 closes, and the two findings its second pass left behind ([2c20d58](https://github.com/murat-akpinar/k8rs/commit/2c20d58acf4154ba2ca797401551acbad1df950e)) — The security gate and the closing second pass, the last two steps of the ritual that had not run. Both green; neither found a blocker.
- *(docs)* Phase 4 gets its families, and the freeze meets a card the capture proves wrong ([453a449](https://github.com/murat-akpinar/k8rs/commit/453a449e0deb413659f19c16a3f1998f1186dedc)) — Phase 4's first box needs rule 6 changed and `rules.rs` froze at Phase 3 close, so the next session would have met the collision cold on box one. D124 rules it: the forward-only freeze exists so nothing built on a layer finds the layer moved — `Finding`, `ObjectId`, `analyze`'s signature — and it was never a rule that a wrong card becomes permanent because the phase that drew it ended.
- *(ui)* The six panes, and the one place a missing metrics-server is said ([10b8580](https://github.com/murat-akpinar/k8rs/commit/10b8580aec0a094e0e2486d103372b545832cb29)) — Family C's screen spec, ahead of its code: D127 owed three things to screens/ before the report boxes could be briefed, and a brief cannot decide behaviour the screen contradicts.
- *(rules)* The reports cannot see the helpers written for them ([25b6823](https://github.com/murat-akpinar/k8rs/commit/25b6823e910385e369c5ed5abae64a6bc61b12ae)) — Family C stopped on its first box, and correctly. Five of the six reports call functions that exist, are correct, and are private to rules.rs, which is analysis.rs's sibling and not its parent — thirteen E0603s, proven by the compiler and reverted. Two of them are in no Vec<Finding> at all: analyze's node loop never calls N4 or N5, which were written to be the Versions and Capacity reports' input and were reachable only from their own tests.
- *(docs)* Record the node-name regex wider than its own comment ([cd78cfc](https://github.com/murat-akpinar/k8rs/commit/cd78cfc1e7ae8e0a72fd61f051344a4b67e3fde5))
- *(docs)* Triage the boxes that were injected into an open phase 5, and count them again ([bd4aea1](https://github.com/murat-akpinar/k8rs/commit/bd4aea1c504d2099db75c04e867e880a5b25a255)) — Eight of the ten go to backlog.md, which is where a finding needing a ruling lives; two stay because a guard of theirs goes vacuous exactly when connect() lands, and both say so in their own bodies.
- *(docs)* Re-open two boxes checked over work their own text does not describe ([bba0003](https://github.com/murat-akpinar/k8rs/commit/bba0003832d859ba365b9218c6c12406ca9d80a9)) — A whole-project review — an operator over the product files, a test engineer over the suite and the guards, an outside reader over the process — found two `[x]` boxes whose own done-when is not met, and no new work is boxed anywhere.
- *(docs)* Make phase 4's owed re-close visible to the next session ([bd089e6](https://github.com/murat-akpinar/k8rs/commit/bd089e621722ee36149ea1ec8a82b9554f4148cf))
- *(docs)* Close phase 4 a second time, and re-take the counts a close owns ([84eed4c](https://github.com/murat-akpinar/k8rs/commit/84eed4c3ce8ec442f4cfd615a47f980bb665aae8)) — The re-close ran the whole of CLAUDE.md's phase-close ritual rather than a diff of it, which is D157's rule, and D157's shape held again: the family review was pointed at the boxes before any rule, and two of the five findings came from there. Five findings from the review and two from the closing second pass, none blocking, so the phase closed on all seven.
- *(docs)* Give the token-hygiene box the enum that already landed ([ec146f7](https://github.com/murat-akpinar/k8rs/commit/ec146f73eb8204e1a2842f7783ed0a508e89ccd1))
- *(docs)* Correct the phase 5 gate row the token-hygiene box overturned ([4f52616](https://github.com/murat-akpinar/k8rs/commit/4f526163250bb88177a50fe3378dea12c684e653)) — The gate still read "Debug is wrapped on anything that could hold it", which is the rule D164 replaced: the ban is on the derive, because a hand-written impl leaves {:?} compiling forever and the guard cannot tell whether one leaks. It is the row somebody ticks at phase close, so a stale copy here is worse than a stale sentence in prose - it also names the mechanical half and the hand-checked half, which the old wording ran together.

### 🧪 Testing

- *(fixtures)* Prove the predicates that decide a fixture is trustworthy ([03a1deb](https://github.com/murat-akpinar/k8rs/commit/03a1deb1c12d23cee7d38418fe297d4c628ac929)) — cluster.sh verify holds one jq predicate per fixture, and each decides whether that fixture reached the state its rule is about. They had only ever run against a live cluster, where being wrong is invisible both ways: too loose passes a pod in the wrong state, too tight burns the 420s timeout and fails a correct one — and jq cannot tell a broken filter from an unmet condition either, since both exit non-zero.
- *(fixtures)* Add the broken pod that has an owner, and fix a predicate that named one instant of a loop ([27c8443](https://github.com/murat-akpinar/k8rs/commit/27c8443319f050e53b51f95d5daab1228adf55d9)) — broken-owned is a Deployment whose pod crashes, so one object carries both halves of the owner chain: pod -> ReplicaSet -> Deployment. Every other pod fixture is bare, leaving the grouping key tested only in its no-owner case.
- *(fixtures)* Add the shapes the first capture could not produce, and the node states behind them ([31e11c9](https://github.com/murat-akpinar/k8rs/commit/31e11c9d7c3e930df153b61e7a0d1ccb759f4400)) — Thirteen shapes land in the two manifests: a StatefulSet at all, a Deployment caught mid-rollout, a DaemonSet whose pods cannot start, a resize the node cannot fit, a pod-level limit the container never declared, a subPath and a second container on the hostPath mount, a termination message the kubelet actually wrote, and on the healthy side a read-only host mount, a native sidecar, a pod-level request and an init container with resources. `broken-pending` is respun on a nodeSelector so N6's pod side has a real object.
- *(fixtures)* Capture the shapes the manifests were written for, and repin now ([1effe7b](https://github.com/murat-akpinar/k8rs/commit/1effe7b5bcb0a855458616cc5faf840a50b81295)) — The second trip ran end to end against kindest/node:v1.36.1: break -> verify (23/23) -> just fixtures (34 fixtures, then break-nodes and 3/3 node predicates) -> unbreak -> cluster-down. Four Phase 2 boxes close with it, and the capture was read before the teardown so a missing shape could still have been re-taken against a live cluster.
- *(rules)* Retire the syntheses onto the captured objects ([a68044f](https://github.com/murat-akpinar/k8rs/commit/a68044ff7ce6882295fd19e05324933aa343a110)) — Twelve tests that planted a shape on a decoded copy now read a real capture: rule 2's recency clause on a serving container, rule 5's CRITICAL band and its serving half, the init loop that retried and succeeded, the unready sidecar, rule 14's positive, rule 13's storage branch, the Succeeded and Failed skips, rule 6's exit 0, 143 and 126/127 arms, the runtime-socket escalator, and From<StatefulSet>, whose doc had said no test could cover it yet.
- *(rules)* The run a container is sitting in is no rule's subject ([e98a48b](https://github.com/murat-akpinar/k8rs/commit/e98a48b10d78c084a123c04fa0bc678f6cd0aef4)) — `state.terminated` — the run a container is stopped in right now, as opposed to `lastState`, the run before this one — draws no card and is read by one function, which may only suppress. The box asked whether that is a decision or an omission. It is a decision, and this makes it load-bearing: a test section whose red run is a reader being *added* to the rule set.
- *(rules)* The mutation gate was run for the first time and it is red ([36c5d92](https://github.com/murat-akpinar/k8rs/commit/36c5d9281e97b9774be866157caed5174446f2cc)) — 519 mutants over rules.rs in 19 minutes: 271 caught, 229 unviable, 19 missed. Ten are in the humanised duration, where no unit boundary is asserted, and six sit on the NOT_READY_GRACE comparison, which is read either side of the threshold and never at it. The box stays open with the locations recorded.
- *(ci)* The mechanizable half of the security gate is a script now ([a6e66a0](https://github.com/murat-akpinar/k8rs/commit/a6e66a041776ba84c3506b38c957842704c6c80f)) — Six checks a human was re-reading on every diff: workflow permissions and SHA-pinned actions, no shell spawned from src/, no dependency or hostname outside the ten, no Debug derived over a token holder, no door into the in-cluster environment, no TLS knob turned off by us. Three of them guard a class that is empty today and exist so it stays empty when $EDITOR and kube arrive; none was written for a feature that does not exist, and D105 lists what that leaves human-checked with the phase that makes each one mechanical.
- *(ci)* Guard todo.md's structure, and give the guards one list ([9a36f7a](https://github.com/murat-akpinar/k8rs/commit/9a36f7a4a9791d02d911a79c0684fc1c5c04ef94)) — A compaction deleted the `## Phase 3` heading from todo.md together with the phase goal and Phase 2's done-when and frozen-after, and everything under them survived, so the loss was invisible box by box: the file read as though Phase 2 ran on for another 130 lines and "the first unchecked box in the lowest open phase" pointed into a closed one (a9e9397). check-docs.py could not see it — nothing linked to that heading, so no anchor broke.
- *(ci)* The init predicate certified a healthy capture as crash-looping ([3d2a3f0](https://github.com/murat-akpinar/k8rs/commit/3d2a3f0d7f5b677756c6c0646f88117c338aada8)) — [init] read `waiting CrashLoopBackOff or lastState==1` where its two siblings read `lastState==1 and (waiting or terminated==1)`, so the second disjunct said nothing about the current state. It passed a running init container — and it passed healthy-retry.json, a committed capture of a healthy pod whose init loop finished. Now the same shape as [crashloop] and [owned].
- *(rules)* A container backing off before its first restart was told it has zero restarts ([71aebe4](https://github.com/murat-akpinar/k8rs/commit/71aebe4d588386a02e4d9f60275f0942b03d6cef)) — `crash_looping`'s `if c.restarts > 0` was MISSED by every mutation run this phase has done, in three line positions. Flipped to `>= 0` it ships `0 restarts` on a real card — a fact line arguing with its own title, since "nothing has crashed" is what a zero count reads as under *CrashLoopBackOff* — and nothing went red, because no committed capture holds a container that is backing off before its first restart. The window is one backoff wide: the kubelet stamps `restartCount` on the instance when it creates it, so the first instance carries `0` while its failed run has already moved down into `lastState`.
- *(rules)* The duration ladder was asserted at no rung boundary, and fourteen other places a test could not fail ([487ac34](https://github.com/murat-akpinar/k8rs/commit/487ac347f161881d2181386633942e20cc172949)) — Two Phase 3 boxes, one family: the per-rule positive/negative audit and the mutation gate over rules.rs. The audit found no gaps — 21 of 21 rules already had both — and the sweep found fifteen, ten of them the same defect.
- *(ci)* Turn four promises into build steps and anchor the node-name refusal ([9fb33a1](https://github.com/murat-akpinar/k8rs/commit/9fb33a11129e5f828ef6ed45bf12ebb50de6ca01)) — Family A of Phase 4, five boxes, all in scripts/.
- *(fixtures)* The objects two reports exist to catch, and had none ([4635e09](https://github.com/murat-akpinar/k8rs/commit/4635e09f1039214984ce6d6aa028a5d7b3fb23d8)) — Drain safety's whole reason for existing — a PodDisruptionBudget whose minAvailable equals the replica count — had no object to be proven on, and Waste's headline row, the Service matching no pod, had none either. poddisruptionbudgets.json and persistentvolumeclaims.json were both empty lists and endpointslices was not captured at all.
- *(fixtures)* The object that lets a frozen sum be proven wrong ([8325b7e](https://github.com/murat-akpinar/k8rs/commit/8325b7e64505fbac7dd4100404d202577c7e7971)) — Box 1753's second field, spec.overhead, counts toward nothing today: the sum that would read it is frozen, and changing it is D124's question, whose first condition is a defect proven on a committed capture. No captured pod has the field, because kind runs runc and nothing sets a RuntimeClass — so the box could not close on its own stated reason.
- *(fixtures)* A corpus the reports can be joined against ([b5229e1](https://github.com/murat-akpinar/k8rs/commit/b5229e1982a4b37f4c3ecb08d1ce1781ff8a4b09)) — Four capture runs. The first committed two fixtures on a container face their own tests cannot read, because the guard on the committed bytes was spelled like the predicate on the live pod; the second landed and then showed that no captured pod carried either PodDisruptionBudget's label, so Drain safety's join had a budget on one side and nothing on the other; the third closed that; the fourth pinned the node of the object built to make a per-node sum legible, which the break sequence was emptying.
- *(ci)* Check every file that pins an instant against the committed certificates ([6884c8b](https://github.com/murat-akpinar/k8rs/commit/6884c8b411f5f5ff94d378f7454ff0fac71e0f75)) — The guard read `src/rules_tests.rs` only. `src/analysis_tests.rs` now pins its own instant, and the Certificates report's badge and rows are all measured from it against the same bytes — so a moved pin invited the repair the file exists to prevent, editing `15d` to match.
- *(rules)* Pin the role spelling to the function, not to its words ([9507b5c](https://github.com/murat-akpinar/k8rs/commit/9507b5c15bf8b70771d0cc86ea2fed1eb37023e0)) — The Restarts row asserted container_fact's current sentence as a literal, so a producer that inlined the same words passed every test and every mutant — measured, not argued: with the producer inlined and the gloss reworded, a card and a report row said two different things about one sidecar and the suite stayed green. The expectation is now composed through the function, with a canary that a gloss exists at all.
- *(rules)* Close Phase 3 a second time, and record what a re-close runs ([2d0db63](https://github.com/murat-akpinar/k8rs/commit/2d0db6390a77b5839d853d97d7dc320ff2a13a29)) — Phase 3 closed on 2026-08-20 and was re-opened two days later because one checked box's own done-when was not met. That box has landed, so the ritual runs again — and the ritual had no rule for the second time, which D157 now writes: a re-close runs the whole list rather than a diff of it, because a box checked over work narrower than its own text is invisible to anything a diff scopes.

### ⚙️ Miscellaneous Tasks

- Init ([542deb0](https://github.com/murat-akpinar/k8rs/commit/542deb041294e191d6af55acc0e1aef3b246cfa9)) — The whole design phase: decision record, requirements, screens, build plan and the working rules. No code yet.
- *(docs)* Pin a model to each subagent, by the weight of its job ([12aaab4](https://github.com/murat-akpinar/k8rs/commit/12aaab4d1e2fe30365f11af91a3b014846682091)) — The four agents that write product code, guards or the blocking review run on Opus; the screen designer, which writes ASCII and wording and no Rust, runs on Sonnet. Without the field every agent inherited whatever the main session happened to be set to, so the review that is meant to catch a lie could silently be the cheapest model in the house.
- *(ci)* Run the cross-compile matrix from just check ([5490d26](https://github.com/murat-akpinar/k8rs/commit/5490d26d5a4d1f63139de0cffdc1321df62363df)) — CI cross-compiled the four release targets and `just check` ran nothing equivalent, so a break that only appears for musl or darwin was discoverable only after a push — the exact failure "`just check` is the whole of CI, or it is a lie" exists to prevent, and cross-compilation breaks at link time and late.
- *(docs)* Add the /basla command that runs boxes back to back ([ba017a0](https://github.com/murat-akpinar/k8rs/commit/ba017a0c6601d44656a36327e4beabe24b7505fd)) — D98 already authorised the PM to run boxes without approval; what was missing was a trigger. /basla reloads the procedure, recovers a box left in flight by a crash before picking a new one, and names the three stops that remain.
- *(ci)* The unattended run had two allowed commands and needed the cycle's own ([c80e607](https://github.com/murat-akpinar/k8rs/commit/c80e6076ca83613bff5e521b034f5e3777580e0a)) — `claude -p "/basla"` cannot answer a permission prompt, so anything not on the allowlist is refused rather than asked — and the project had two entries, one of them a `tmux ls`. A night's run would have stopped before its first `just check`.
- *(ci)* The sweep the unattended run must wait for is longer than a foreground call ([d56ccf7](https://github.com/murat-akpinar/k8rs/commit/d56ccf7c0ffc41e90748769b81679aacdf5c9817)) — `/basla` tells the unattended run never to end a turn on background work, because a `claude -p` process exits and takes every background job with it. That rule was written on its own and cannot be obeyed for the one gate it was written about: a foreground call is capped at ten minutes, and the whole mutation sweep is longer than one.
